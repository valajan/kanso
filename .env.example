GITHUB_APP_ID=
GITHUB_WEBHOOK_SECRET=
GITHUB_PRIVATE_KEY_PATH=./perfguard.pem
PORT=3000

# OpenAI API key used by src/ai-analysis to generate a remediation
# section in the GitHub comment when a Lighthouse metric is in the red
# AND the regression vs main exceeds 10%. Optional — if unset, the AI
# analysis is silently skipped without breaking the main pipeline.
OPENAI_API_KEY=
